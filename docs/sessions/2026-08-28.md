# Session — 2026-08-28

## 建立项目长期记忆系统

从中文版项目记忆包建立：

```text
WHY     → decisions/
WHAT    → PROJECT / product/
NOW     → CURRENT / handoff/
MAYBE   → IDEA_LEDGER
HISTORY → HISTORY / sessions/
```

使用独立分支 `docs/project-memory-v1`，基于 `origin/work/universal-capture-beta15` @ `ec854a9`。初始配置阶段保持 local-only，不修改 `src/`、不创建 PR、不 merge；随后经用户明确授权，已 Push 到 `origin/docs/project-memory-v1`。当前仍未合入 beta.15 开发基线。

## GitHub 动态事实校准

重新核对：

- Development：`work/universal-capture-beta15` @ `ec854a9d5ca813e97f5c4f48b80f2afc3bf8de56`
- Preview：`preview/go-study-0.3.0-beta.15` @ `11acbda84255c1df3cdf56b9f46a689c7a5b1066`
- Preview Release：已发布
- Preview CI run `33065679457`：失败

CI 失败不是“测试全红”，而是 release check 构建后发现生成 `main.js` 与提交内容不一致。历史 `288 / 288` 只能作为 checkpoint。

## Freeform 真人验收发现

未收录视频生成的 beta.15 回链使用：

```text
obsidian://go-study?mode=freeform&path=...
```

Obsidian 会把 `path` 当成 Vault 路由字段，出现 `Vault not found`。GitHub 当前 beta.15 仍未修复。

曾在本机存在 `locator` + Obsidian 内点击拦截候选，但未上传、未完成可信真人验收。曾出现把 public manifest 错装到 Preview 目录的打包错误，该尝试不能作为功能证据。

## 未决

Freeform 最终采用非保留参数的 `obsidian://go-study`，还是仅 Freeform 使用直达 `jv://`，仍为 `PROPOSED`。不得借此恢复 AHK / Companion 作为正常 Runtime 的必需依赖。


## EPHEMERAL / LOCAL ONLY 修复候选（历史线索）

曾在本机存在 `fix/beta15-freeform-reopen` 候选分支：

- `27d43dc`：Freeform URI 使用 `locator`，并在 Obsidian 内拦截普通点击；
- `105543c`：Windows 换行符测试兼容；
- `92137d8`：旧 Windows PowerShell 部署兼容。

这些 branch / commit 当时**未上传到 GitHub**，因此只作为历史排查线索，不属于可恢复的项目事实。未来 Agent 不得假定它们在新环境中存在；若继续该方向，应从当前正确 beta.15 远程基线重新验证并形成可恢复的远程提交。


## beta.16 Freeform jv 修复候选

重新审查 beta.15 Freeform 回链后确认根因：

```text
obsidian://go-study?mode=freeform&path=...
```

其中 `path` 与 Obsidian 自身 Vault 路由参数冲突，插件 protocol handler 可能尚未接管就先出现 `Vault not found`。

同时核对旧 `markdown2potplayer` 实现，确认原协议为：

```text
jv://open?path=<media>&time=<HH:MM:SS>
```

而 Go Study 自身 `toPotPlayerUri()` 也已经使用同一 `jv://` 语义。

因此建立隔离修复分支：

`fix/freeform-jv-reopen-v1`

策略：

- Managed：保持 `obsidian://go-study?resource=...` + Resource ID + Resolver；
- New Freeform：直接输出 `jv://open?path=...&time=...`；
- Old beta.15 Freeform：在 Obsidian 文档点击层尝试抢先拦截并交给现有 `openFreeformReference()`；
- 不恢复 Companion polling，不把 AHK/Companion 变成整个产品的必须依赖。

自动化结果：

- 289 / 289 tests PASS；
- build PASS；
- release validation PASS；
- committed `main.js` consistency PASS；
- isolated `go-study-preview` package PASS；
- GitHub prerelease 与资产验证 PASS。

发布：

- `Go Study Preview 0.3.0-beta.16`
- Tag：`go-study-preview-v0.3.0-beta.16`
- 状态：等待 Windows + Obsidian + PotPlayer 真人验收。


## beta.17 Portable Freeform

用户明确提出长期目标：

> 同一篇笔记从 macOS 到 Windows，链接协议本身都应该可理解和可处理，不能把 Windows 专属 `jv://` 暴露成永久笔记协议。

因此从 beta.16 建立：

`work/portable-freeform-beta17`

核心决策：

- Managed Link 保持 `obsidian://go-study?resource=...`；
- 新 Freeform 使用 `obsidian://go-study?mode=freeform&locator=...&name=...&position=...&v=2`；
- `locator=` 取代 Obsidian 保留 `path=`；
- `jv://` 退回 Windows 内部执行兼容层；
- 新 Freeform 可通过唯一媒体文件名在另一设备上尝试升级为已收录 Managed Resource；
- macOS/Linux 接受 POSIX 本地路径并可调用系统播放器；
- 本地未收录视频跨设备时，如果另一台设备路径不同且未收录，不能凭空解析，需未来 Path Mapping / multi-locator。

同时修复 beta16 链接标题乱码：

`↗ 回到课程 · 时间`

自动化：

- 293 tests PASS；
- release:check PASS；
- committed `main.js` consistency PASS；
- isolated Preview packaging / prerelease / asset verification PASS。

Preview：

- `Go Study Preview 0.3.0-beta.17`
- Tag：`go-study-preview-v0.3.0-beta.17`
- Draft PR：#25
- 真人验收：PENDING。

## beta.17 Windows 真人验收结果

用户实机反馈：

1. 新 Freeform beta.17 链路可用；
2. 视频后来收录后，历史 Freeform 链接可正常升级到 Managed 行为；
3. beta.16 / beta.15 旧链接兼容可用；
4. 已收录视频仍输出 Managed Resource-ID 回链；
5. Freeform 可见标题仍有乱码显示问题，功能正常但需要 polish。

因此：

- beta.17 Windows 核心回链方案视为功能 PASS；
- ADR-007 暂不升 CONFIRMED，因为 macOS / cross-device local path 尚未真人验证；
- 跨平台本地路径映射延期到后续单独开发；
- 先进行 beta.17 交互小优化，再评估 beta.18 Companion Note Window。


## beta.18 笔记小窗视觉规格确认

用户提供 PotPlayer 实际截图作为尺寸参考。

确认：

- 小窗默认视觉大小接近 PotPlayer 右侧播放列表栏；
- 目标是覆盖 / 替代右侧区域，而不是强制操作系统分屏；
- 窗口可以很窄，但内部仍是完整真实 Markdown 编辑器；
- 通过 compact scale / UI 密度压缩实现“小而完整”，不做截图式缩略图；
- 只保留 Note 编辑区，隐藏普通 Obsidian 导航 Chrome；
- 用户可拖动 / resize；
- 保存最后 geometry；
- 支持保存多套布局 preset；
- 继续围绕 locked Companion Note 作为 Capture Target。

正式规格：`docs/product/companion-note-window.md`


## beta.18 实现与 Preview 发布

用户确认 beta.18 边界后，同一轮完成 beta.17 polish 与 beta.18 Companion Note Window MVP。

开发：

- Branch：`work/companion-note-beta18`
- Draft PR：#26
- Final validation HEAD：`28208c9e244d0fcbe4fb1c7132bb800da09b0d67`

beta.17 polish：

- HUD 同方向双击直接执行；
- Quick Note popup 可拖动、位置记忆、默认位置下移、滚动条弱化；
- Freeform 可见标题稳定为“回到课程”；
- HUD 设置压缩；
- Template editor + live rendered preview 相邻。

beta.18：

- 真实 Obsidian Markdown pop-out leaf；
- 默认播放器右侧栏布局；
- move / resize / scale；
- Geometry persistence；
- custom layout presets；
- Locked Companion Note 优先成为 Capture Target；
- commands + settings；
- Companion-only compact CSS。

自动化：

- 307 / 307 tests PASS；
- release:check PASS；
- committed main.js consistency PASS；
- final CI #188 PASS；
- Preview publisher validation/package/upload/assets verification 全部 PASS。

发布：

- `Go Study Preview 0.3.0-beta.18`
- Tag：`go-study-preview-v0.3.0-beta.18`
- Release Target：`7eb7fa2f66e7b8df03335e168d0c4ed6e48d6cc0`

状态：

**等待真实 Windows + Obsidian beta.18 acceptance。**


## Legacy Next / Go Study protocol 冲突定位

故障现象：

- 旧 Vault 中 Go Study 无法正常启用；
- 相同 Go Study 插件在新 Vault 正常；
- 移除 / 重建 Go Study data 与 workspace 未解决；
- 停用 `learning-resource-hub-next` 后 Go Study 立即恢复。

本地代码排查定位：

- 两代插件均注册 `go-study` Obsidian protocol action；
- 完整 reload / cold start 时存在重复 handler registration；
- 昨晚能同时启用不等于安全共存，可能只是当时加载顺序 / 运行时状态没有触发冲突。

结论：

- 插件包本体没有坏；
- 时间戳 CSS 不是根因；
- 当前属于 legacy coexistence bug。

临时处理：

- legacy Next 与 Go Study Preview 只启用一个；
- 不立即改 protocol 名，避免破坏已有回链；
- 后续为 Go Study 增加 fail-safe registration + legacy coexistence guard。


## beta19-A Study Mode Shell

用户确认：

- 点击笔记 = 普通学习；
- 拖动笔记到右侧独立小卡片 = 学习模式；
- Study Mode 选择的笔记作为本次视频学习目标，不形成永久 Resource → Note 绑定；
- Companion 默认置顶，并提供图钉切换；
- 保持当前 right-rail 默认布局；
- 关闭小窗即退出学习模式，但不关闭 PotPlayer；
- 点击小窗属于主动深度整理场景；用户切回 PotPlayer 后恢复 HUD-first 流程；
- 暂不实现 Player Binding，采用方案 A；
- Companion 标题栏只保留笔记名，去除 Obsidian / Vault 冗余标题；
- UI 未来数轮以视觉优化为主，功能主干已基本满足工作流。

实现：

- branch `work/study-mode-beta19a`
- Draft PR #27
- final HEAD `7002b1c14fcd97e15f4578dbd921e41e5c06a999`
- CI #195 PASS
- 314/314 tests PASS
- main.js consistency PASS


## beta19-A 实机通过 / beta19-B 启动

用户反馈 beta19-A “没问题，很顺手”，因此 Windows 核心行为验收通过。

新发现：

1. 尚未把“未收录零散视频 / Freeform”完整带入 Study Mode；
2. 需要审计 Freeform 时间戳兼容：
   - 普通点击回 PotPlayer；
   - Ctrl+点击浏览器打开；
   - Bilibili 网络视频能否打开对应时间点。

代码审计结果：

- beta19-A 已支持 Freeform HTTP(S) 在 Ctrl+点击时打开浏览器；
- 但 Ctrl+点击仅打开原始网页 URL，尚未应用 Capture 的 `position`；
- Freeform v2 schema 已支持可选 `web` 字段，但生成 Markdown 时此前没有写入；
- Bilibili URL 时间线适配尚未实现。

beta19-B 已建立：
- branch `work/study-mode-beta19b`
- Draft PR #28

首个实现切片：
- Freeform v2 写入可选 web source；
- Ctrl+点击 Bilibili URL 时保留 `p` 并写入 `t=<seconds>`；
- 非 Bilibili HTTP URL 保持原样；
- 普通点击 PotPlayer 行为不变。

未收录零散视频进入 Study Mode 的 UI 入口仍需最终确认后接入。


## beta20 悬浮时间线

用户确认最终视觉方向：

- 越轻量越好；
- 并非所有用户都需要，因此放在“视频笔记增强”中作为可选开关；
- 默认状态仅右边缘细线 / 小点；
- Hover 才展开一点；
- 透明背景；
- 拒绝卡片式 Timeline。

同时确认最后功能增强需要支持：

- 一个笔记内识别多个视频来源；
- 不同视频的时间点分开展示；
- 时间点可点击回跳；
- Freeform 本地视频自动记住标题 / 文件来源；
- 为后续通过视频反查相关笔记留下 metadata 基础。

实现：

- branch `work/timeline-navigator-beta20`
- Draft PR #29
- HEAD `91916d440b30ca5c95bd191ee2bc9258949aa12c`
- CI #209 PASS
- 325/325 tests PASS
- Preview `0.3.0-beta.20` 已发布。


## beta20.1 Timeline 实机挂载修复

用户确认两个 Timeline 相关开关均已开启，但测试笔记中完全没有出现右侧 rail，因此按 bug 处理。

修复策略：
- rendered-link fallback；
- document.body fixed overlay；
- 避免 Markdown editor stacking/overflow 隐藏；
- viewport scroll/resize 重定位。

发布：
- Preview 0.3.0-beta.20.1
- 327/327 tests PASS
- Draft PR #30
