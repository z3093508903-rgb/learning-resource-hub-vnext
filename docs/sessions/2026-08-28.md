# Session — 2026-08-28

## 建立项目长期记忆系统

从中文版项目记忆包建立：

```text
WHY     → decisions/
WHAT    → PROJECT / product/
NOW     → CURRENT / handoff/
MAYBE   → IDEA_LEDGER
HISTORY → HISTORY / sessions/
```

使用独立分支 `docs/project-memory-v1`，基于 `origin/work/universal-capture-beta15` @ `ec854a9`。初始配置阶段保持 local-only，不修改 `src/`、不创建 PR、不 merge；随后经用户明确授权，已 Push 到 `origin/docs/project-memory-v1`。当前仍未合入 beta.15 开发基线。

## GitHub 动态事实校准

重新核对：

- Development：`work/universal-capture-beta15` @ `ec854a9d5ca813e97f5c4f48b80f2afc3bf8de56`
- Preview：`preview/go-study-0.3.0-beta.15` @ `11acbda84255c1df3cdf56b9f46a689c7a5b1066`
- Preview Release：已发布
- Preview CI run `33065679457`：失败

CI 失败不是“测试全红”，而是 release check 构建后发现生成 `main.js` 与提交内容不一致。历史 `288 / 288` 只能作为 checkpoint。

## Freeform 真人验收发现

未收录视频生成的 beta.15 回链使用：

```text
obsidian://go-study?mode=freeform&path=...
```

Obsidian 会把 `path` 当成 Vault 路由字段，出现 `Vault not found`。GitHub 当前 beta.15 仍未修复。

曾在本机存在 `locator` + Obsidian 内点击拦截候选，但未上传、未完成可信真人验收。曾出现把 public manifest 错装到 Preview 目录的打包错误，该尝试不能作为功能证据。

## 未决

Freeform 最终采用非保留参数的 `obsidian://go-study`，还是仅 Freeform 使用直达 `jv://`，仍为 `PROPOSED`。不得借此恢复 AHK / Companion 作为正常 Runtime 的必需依赖。


## EPHEMERAL / LOCAL ONLY 修复候选（历史线索）

曾在本机存在 `fix/beta15-freeform-reopen` 候选分支：

- `27d43dc`：Freeform URI 使用 `locator`，并在 Obsidian 内拦截普通点击；
- `105543c`：Windows 换行符测试兼容；
- `92137d8`：旧 Windows PowerShell 部署兼容。

这些 branch / commit 当时**未上传到 GitHub**，因此只作为历史排查线索，不属于可恢复的项目事实。未来 Agent 不得假定它们在新环境中存在；若继续该方向，应从当前正确 beta.15 远程基线重新验证并形成可恢复的远程提交。


## beta.16 Freeform jv 修复候选

重新审查 beta.15 Freeform 回链后确认根因：

```text
obsidian://go-study?mode=freeform&path=...
```

其中 `path` 与 Obsidian 自身 Vault 路由参数冲突，插件 protocol handler 可能尚未接管就先出现 `Vault not found`。

同时核对旧 `markdown2potplayer` 实现，确认原协议为：

```text
jv://open?path=<media>&time=<HH:MM:SS>
```

而 Go Study 自身 `toPotPlayerUri()` 也已经使用同一 `jv://` 语义。

因此建立隔离修复分支：

`fix/freeform-jv-reopen-v1`

策略：

- Managed：保持 `obsidian://go-study?resource=...` + Resource ID + Resolver；
- New Freeform：直接输出 `jv://open?path=...&time=...`；
- Old beta.15 Freeform：在 Obsidian 文档点击层尝试抢先拦截并交给现有 `openFreeformReference()`；
- 不恢复 Companion polling，不把 AHK/Companion 变成整个产品的必须依赖。

自动化结果：

- 289 / 289 tests PASS；
- build PASS；
- release validation PASS；
- committed `main.js` consistency PASS；
- isolated `go-study-preview` package PASS；
- GitHub prerelease 与资产验证 PASS。

发布：

- `Go Study Preview 0.3.0-beta.16`
- Tag：`go-study-preview-v0.3.0-beta.16`
- 状态：等待 Windows + Obsidian + PotPlayer 真人验收。


## beta.17 Portable Freeform

用户明确提出长期目标：

> 同一篇笔记从 macOS 到 Windows，链接协议本身都应该可理解和可处理，不能把 Windows 专属 `jv://` 暴露成永久笔记协议。

因此从 beta.16 建立：

`work/portable-freeform-beta17`

核心决策：

- Managed Link 保持 `obsidian://go-study?resource=...`；
- 新 Freeform 使用 `obsidian://go-study?mode=freeform&locator=...&name=...&position=...&v=2`；
- `locator=` 取代 Obsidian 保留 `path=`；
- `jv://` 退回 Windows 内部执行兼容层；
- 新 Freeform 可通过唯一媒体文件名在另一设备上尝试升级为已收录 Managed Resource；
- macOS/Linux 接受 POSIX 本地路径并可调用系统播放器；
- 本地未收录视频跨设备时，如果另一台设备路径不同且未收录，不能凭空解析，需未来 Path Mapping / multi-locator。

同时修复 beta16 链接标题乱码：

`↗ 回到课程 · 时间`

自动化：

- 293 tests PASS；
- release:check PASS；
- committed `main.js` consistency PASS；
- isolated Preview packaging / prerelease / asset verification PASS。

Preview：

- `Go Study Preview 0.3.0-beta.17`
- Tag：`go-study-preview-v0.3.0-beta.17`
- Draft PR：#25
- 真人验收：PENDING。

## beta.17 Windows 真人验收结果

用户实机反馈：

1. 新 Freeform beta.17 链路可用；
2. 视频后来收录后，历史 Freeform 链接可正常升级到 Managed 行为；
3. beta.16 / beta.15 旧链接兼容可用；
4. 已收录视频仍输出 Managed Resource-ID 回链；
5. Freeform 可见标题仍有乱码显示问题，功能正常但需要 polish。

因此：

- beta.17 Windows 核心回链方案视为功能 PASS；
- ADR-007 暂不升 CONFIRMED，因为 macOS / cross-device local path 尚未真人验证；
- 跨平台本地路径映射延期到后续单独开发；
- 先进行 beta.17 交互小优化，再评估 beta.18 Companion Note Window。


## beta.18 笔记小窗视觉规格确认

用户提供 PotPlayer 实际截图作为尺寸参考。

确认：

- 小窗默认视觉大小接近 PotPlayer 右侧播放列表栏；
- 目标是覆盖 / 替代右侧区域，而不是强制操作系统分屏；
- 窗口可以很窄，但内部仍是完整真实 Markdown 编辑器；
- 通过 compact scale / UI 密度压缩实现“小而完整”，不做截图式缩略图；
- 只保留 Note 编辑区，隐藏普通 Obsidian 导航 Chrome；
- 用户可拖动 / resize；
- 保存最后 geometry；
- 支持保存多套布局 preset；
- 继续围绕 locked Companion Note 作为 Capture Target。

正式规格：`docs/product/companion-note-window.md`
