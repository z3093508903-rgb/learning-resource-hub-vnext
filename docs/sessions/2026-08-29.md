# Session · 2026-08-29

## beta20.5 Timeline 逻辑修正

用户确认 beta20.4 后悬浮时间线已能稳定显示，不再闪烁。

新产品边界：

- Timeline 的目的 = 找知识点 / 定位笔记；
- Timeline 时间点点击 = 跳到当前笔记对应行；
- Timeline 不应该直接打开网页或播放器；
- 正文“回到课程”链接继续承担媒体回跳；
- Timeline 必须严格限定在当前相关 Markdown，不能在其他页面全局残留。

实现：
- branch `fix/timeline-note-navigation-beta20-5`
- Draft PR #34
- Preview 0.3.0-beta.20.5
- 337/337 tests PASS


## beta20.6 发布前 P0 收口

用户最终确认：
- 时间戳正文默认只显示数字；
- 小胶囊样式由仓库 CSS 提供；
- 不加 tooltip；
- 视频标题不进正文，只作为后台来源 metadata；
- Timeline 类似标题导航，只对 Go Study 时间戳负责；
- Timeline 折叠点数按视频来源数量显示；
- Hover 展开后显示来源 + 时间点；
- 项目导航 Markdown 笔记必须支持拖入同款 Study Mode 入口，用当前已经打开的零散 PotPlayer 视频学习。

实现：
- timestamp-only default + legacy-default migration；
- compact timestamp capsule CSS；
- source-count Timeline rail；
- navigation Markdown drag source；
- background current PotPlayer probe；
- Freeform Study Mode context；
- final release acceptance checklist。

Preview：
- 0.3.0-beta.20.6
- 345/345 tests PASS
- Draft PR #35


## beta20.7 原生拖动与小窗光标

实机反馈：
- P0 的零散视频拖动入口只在 Go Study 工作台项目笔记出现；
- 用户真正希望 Obsidian 原生文件树和已打开标签页里的 Markdown 也可拖进 Study Mode；
- Companion 小窗鼠标点击位置与光标落点不自然，难以准确控制输入位置。

处理：
- 增加原生文件树 / Markdown tab 的全局 drag bridge；
- HTML5 drag + pointer drag 双路径；
- 与工作台拖动入口去重；
- Drop 仍绑定当前已打开 PotPlayer，不重新播放、不收录；
- 移除 Companion CodeMirror 容器 CSS zoom，改用字体缩放保持紧凑。

Preview beta20.7：349/349 tests PASS，Draft PR #36。


## beta20.8 数据安全事故修复

用户反馈：
- 测试数据损失有限，可以直接重构安全边界；
- 旧备份入口实际上难以找到，未形成可恢复的真实自动备份体系。

处理：
- 启动前 raw data.json snapshot；
- external `.obsidian/go-study-recovery`；
- loadData 空状态 fallback；
- populated → empty persist guard；
- rolling pre-save snapshot；
- 设置页可查看真实 data.json / backup path；
- “立即备份 / 打开备份文件夹 / 恢复最近备份”。

Preview 0.3.0-beta.20.8，356/356 tests PASS，Draft PR #37。


## beta20.9 Portable Backlink 与发布前补强

用户确认 beta20.8 数据安全轮可继续下一轮，并提出：

- 旧 Resource ID 回链在重装 / 状态缺失后失效，需要不依赖完整同步的兜底；
- 完整跨设备 / 账号同步先记录、不实现；
- Bilibili Managed timestamp Ctrl+点击应打开浏览器对应时间；
- 浅色模式 Add / Import Modal 有白字白底；
- 手动备份需要命名 / 重命名并永久避开自动 retention。

实现：
- Managed v3 self-describing backlink；
- legacy v1 recovery snapshot lookup；
- legacy v1 one-time Resource relink alias；
- Managed + Freeform Ctrl-click browser resolution；
- Bilibili p+t；
- Timeline missing-resource title fallback；
- light-mode modal theme scope；
- named `saved-*` backup，不参与自动 retention。

Preview：
- beta20.9
- Draft PR #38
- 367/367 tests PASS
- release target 71773c2f54550d0d59f991c09ba3de2c80171da9

仍未关闭：
- Obsidian 原生文件树 / 已打开 Markdown tab 拖入 Study Mode。
